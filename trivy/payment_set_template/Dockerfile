FROM alpine:3.20
WORKDIR /app

# create a fake secrets to detect for trivy
ARG STRIPE_SECRET={{GENERIC_API_KEY}}
ENV DB_URI={{POSTGRES_URI}}
ENV AWS_ACCESS_KEY_ID={{AWS_ACCESS_KEY_ID}}
ENV AWS_SECRET_ACCESS_KEY={{AWS_SECRET_ACCESS_KEY}}
LABEL jwt="{{DUMMY_JWT}}"



CMD ["sh","-c","echo ok"]
